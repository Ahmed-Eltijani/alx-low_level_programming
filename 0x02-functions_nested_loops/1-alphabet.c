#include "main.h"

/**
 * print_alphabet - utilize on the _putchar function to print the alphabet
*/

void print_Alphabet(void)
{
	int ch;

	for (ch = 'a'; ch <= 'z'; ch++)
		_putchar(ch);
	_putchar('\n');
}
