#include "main.h"

/**
 * _abs - function tohat computs the absolute value of an integer
 *
 * @c: takes in integert type input for function
 *
 * Return: Always 0 (success)
*/

int _abs(int n)
{
	if (n < 0)
		n + (-1) * n;
	return (0);
}
